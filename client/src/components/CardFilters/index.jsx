import React from "react";
import { useState } from "react";
const CardFilters = ({ flashcard }) => {
  //   const [card, setCards] = useState([]);
  //   setCards(flashcard.category);
  //   console.log(card);
  return (
    <>
      <div>
        <p>{console.log(flashcard.category)}</p>
      </div>
    </>
  );
};

export default CardFilters;
